pipeline {
    agent any
    tools { 
        maven 'Maven' 
        jdk 'JDK' 
    }
        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}
